"""Placeholder for RatingPicto — will be fully migrated in a future phase.

Migrated from PHP: src/StaticComponent/RatingPicto.php
"""


class RatingPicto:
    """Maps rating labels to icon URLs."""

    @staticmethod
    def get_picto(rating: str, system: str = "CSA") -> str | None:
        """Return the icon URL for a given rating, or ``None`` if not found."""
        return None
