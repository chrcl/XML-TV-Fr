name: Build Docker image (validation)
on:
  workflow_dispatch:
  pull_request:
    paths:
      - Dockerfile
      - python/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t xmltv-fr .
